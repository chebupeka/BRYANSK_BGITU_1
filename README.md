# Документ за 3 шага

Черновик → тип документа и реквизиты → редактируемый DOCX. Рабочая часть роли 1: FastAPI, стабильный API, OpenAI-совместимый транспорт LLM, конфигурация, ограниченный кэш, Docker Compose и точки подключения модулей команды.

**Начните с [инструкции для роли 1](docs/ROLE_1.md).** Подключение остальных участников: [HANDOFF.md](docs/HANDOFF.md). Запросы и ошибки: [API.md](docs/API.md). Схема приложения: [ARCHITECTURE.md](docs/ARCHITECTURE.md).

**По умолчанию включена заглушка, а не готовый конкурсный MVP.** В режиме `stub` обработчик сохраняет непустые строки исходника без изменения слов и фактов. Режим `openai` проверяет транспорт при штатной политике сохранения исходника. Режим `llm` включает обработку роли 2: модель исправляет орфографию и приводит текст к деловому стилю, а сервис сверяет её ответ с черновиком и не пропускает выдуманные даты, суммы и имена. Интерфейс сообщает, какой режим включён.

Поставка находится в отдельной ветке `feat/role-1-backend-integration`. Для объединения с уже существующими ветками коллег подготовлен [порядок интеграции](docs/INTEGRATION.md): точки подключения, совместные изменения схем и конкретные расхождения проверок.

По умолчанию проект работает без ключей в `stub`: непустые строки исходника сохраняются без исправлений. Режим `openai` действительно вызывает модель, но штатная политика разрешает только сохранение исходного текста и реквизитов. Промпт для делового стиля, извлечение и независимая сверка фактов подключаются участником роли 2. Завершённая смысловая проверка в этой поставке не заявляется.

- React + TypeScript + Vite, компоненты Ant Design и три этапа работы.
- Четыре типа: служебная записка, докладная записка, информационная справка и письмо.
- Два оформления по описанию шаблонов из материалов кейса: «Классический корпоративный» (Times New Roman 14 пт, адресат справа, организация в верхнем колонтитуле, номер страницы в нижнем) и «Современный регламентный» (Arial 12 пт, шапка «Кому / От кого» таблицей, подпись по центру, в нижнем колонтитуле название документа и дата).
- Формы реквизитов из YAML. Вместо недостающих обязательных значений — `[Заполнить: название поля]`; необязательные пустые поля пропускаются.
- Редактируемый DOCX через python-docx. Обязательные реквизиты типов совпадают с перечнем из материалов кейса (`examples/reference/case_reference.yaml` в ветке проверок): у записок — адресат, автор, дата, номер, заголовок, подпись; у справки — заголовок, дата, составитель; у письма — адресат, отправитель, дата, номер, тема, подпись. Необязательные: исполнитель, обращение в письме, адресат справки, приложение, должность подписанта. Реквизиты печатаются как в деловых документах: адресат, строка «дата № номер», заголовок к тексту, подпись, исполнитель. Дата, номер и подписант автоматически не придумываются.
- Реквизиты подставляются из черновика на втором шаге и не повторяются в тексте документа. Берутся подписанные строки («Кому: директору», «Тема — закупка») и записи, которые толкуются однозначно: «Исх. № 12-45 от 11.09.2026» разделяется на номер и дату, дата отдельной строкой считается датой документа, «Приложение на 2 листах» уходит в приложение. Адресата и подписанта из обычного текста не выводим: пустое поле честнее выдуманного.
- Реквизиты привязаны к черновику. Изменили текст — прежние значения стираются, и поля заполняются заново по новому черновику.
- localStorage для черновика, выбранного оформления и реквизитов каждого типа. При ошибке данные остаются в форме, доступна повторная попытка.
- Смена оформления использует уже подготовленное содержание и не вызывает обработчик повторно. Изменение текста, типа или реквизитов сбрасывает подготовленный результат.
- Режим `llm`: локальная модель через OpenAI-совместимый API Ollama, строгий JSON-ответ, один повтор при нарушении схемы или новых фактах, обнуление неподтверждённых реквизитов и предупреждения в интерфейсе.
- FastAPI/OpenAPI, проверки Pydantic, API-тесты и GitHub Actions.

Состав реквизитов и параметры шаблонов сверены с копией эталона кейса, сделанной участником проверок. Там, где эталон молчит, расположение реквизитов взято из ГОСТ Р 7.0.97-2016 в упрощённом виде: блоки идут друг под другом, без угловых бланков в две колонки. Оригиналы описаний из материалов кейса стоит сверить глазами: копия эталона сделана вручную.

## Быстрый запуск через Docker

Нужен запущенный Docker Engine с Compose. Из корня репозитория:

```sh
docker compose up --build
```

Первый запуск не требует `.env`, ключей или модели.

- Интерфейс: [localhost:8080](http://localhost:8080).
- Swagger с рабочими запросами: [localhost:8000/docs](http://localhost:8000/docs).
- Жизнь API: [localhost:8000/api/health](http://localhost:8000/api/health).
- Готовность конфигурации: [localhost:8000/api/ready](http://localhost:8000/api/ready).

Порты доступны только с локального компьютера. Остановка: `docker compose down`.

Проверка сквозного пути через nginx в режиме `stub`:

```sh
python scripts/check_docker.py
```

Для проверки намеренного отказа установите `TEXT_PROCESSOR=unavailable`, перезапустите сервисы и выполните `python scripts/check_docker.py --expected-mode unavailable`. Скрипт не вызывает настоящую модель; другой адрес frontend задаётся `--base-url`.

При обрыве скачивания Python-пакетов повторите `docker compose up --build`: сборка использует увеличенные сетевые таймауты, повторы и кэш BuildKit. Если сбой повторяется, проверьте доступ Docker к `pypi.org` и `files.pythonhosted.org`, включая прокси/VPN. Очистка кэша для обычного повтора не нужна.

## Подключение модели

Скопируйте `.env.example` в `.env` и задайте `TEXT_PROCESSOR=openai`, `LLM_MODEL` и адрес провайдера. Ключ нужен, только если его требует провайдер. Backend отправляет JSON на `{LLM_BASE_URL}/chat/completions`.

Пример переменных для модели, запущенной на вашем компьютере, при backend в Docker:

```dotenv
TEXT_PROCESSOR=openai
LLM_BASE_URL=http://host.docker.internal:11434/v1
LLM_MODEL=ИМЯ_УСТАНОВЛЕННОЙ_МОДЕЛИ
LLM_API_KEY=
```

Замените имя на модель, которая действительно доступна у вашего провайдера. При запуске backend без Docker используйте `http://localhost:11434/v1`. Для внешнего сервиса укажите его OpenAI-совместимый base URL и ключ в `.env`.

Перечитайте настройки, пересоздав оба основных контейнера:

```sh
docker compose up -d --force-recreate
```

`/api/ready` проверяет настройки локально и не обращается к модели. Проверить ключ, сеть и наличие модели можно запросом `/api/process` из Swagger или [examples/api.http](examples/api.http). Режим `openai` оставляет исходный текст как есть: содержательные изменения включает `TEXT_PROCESSOR=llm` — обработчик роли 2 за той же [точкой подключения](docs/HANDOFF.md#роль-2-ии-и-смысл). Его устройство и границы описаны в [ARCHITECTURE.md](docs/ARCHITECTURE.md).

### Необязательная Ollama в Compose

Этот сервис не запускается по умолчанию и не скачивает модели автоматически. После выбора подходящей вам модели:

```sh
docker compose --profile ollama up -d ollama
docker compose --profile ollama exec ollama ollama pull ИМЯ_МОДЕЛИ
```

В `.env` задайте `TEXT_PROCESSOR=openai`, `LLM_MODEL=ИМЯ_МОДЕЛИ`, `LLM_BASE_URL=http://ollama:11434/v1`; ключ можно оставить пустым. Затем:

```sh
docker compose --profile ollama up --build -d --wait
```

В `.env` для обработки роли 2 задайте `TEXT_PROCESSOR=llm` вместо `openai`: режимы используют один и тот же адрес и одно имя модели.

Модели хранятся в Docker volume `ollama_models`. Порт Ollama наружу не публикуется: backend обращается к ней внутри Compose. Остановка всех сервисов профиля: `docker compose --profile ollama down`.

## Настройки

Источник полного списка и пояснений — [.env.example](.env.example). Настройки проверяются при запуске, секреты не возвращаются API.

| Переменная | Значение по умолчанию / назначение |
| --- | --- |
| `TEXT_PROCESSOR` | `stub`; также `unavailable` для проверки отказа, `openai` для штатного транспорта и `llm` для обработки роли 2 |
| `APP_MODULE` | `app.main:app`; точка запуска Compose. Для сборки модулей команды можно указать `app.team_app:app` |
| `LLM_BASE_URL` | Compose: `http://host.docker.internal:11434/v1`; Settings без `.env`: `http://localhost:11434/v1` |
| `LLM_MODEL` | Пустое; для `openai` и `llm` укажите доступную модель, например `qwen2.5:7b-instruct` |
| `LLM_API_KEY` | Пустое; необязательно для локального провайдера |
| `LLM_TIMEOUT_SECONDS` | `20`; сетевой таймаут LLM, не общий дедлайн пользовательского сценария. Локальной модели обычно нужно больше — до `300` |
| `LLM_MAX_TOKENS` | `4096`; предел токенов ответа модели |
| `LLM_MAX_RETRIES` | `1`; допустимы `0` или `1`, повтор только при неверном JSON/схеме/политике |
| `LLM_RESPONSE_FORMAT` | `json_object`; `none`, если провайдер не поддерживает JSON mode |
| `CORS_ORIGINS` | В Compose и `.env.example` — `[]`; для отдельного frontend задайте JSON-массив origins |
| `CACHE_MAX_ENTRIES` | `128`; максимум результатов в памяти одного процесса, `0` отключает |
| `CACHE_TTL_SECONDS` | `300`; время жизни результата в секундах, `0` отключает |
| `MAX_CONCURRENT_PROCESSES` | `4`; предел одновременных обработок, сверх него API отвечает `503` |

Без `.env` локальные Settings разрешают origins `http://localhost:5173` и `http://127.0.0.1:5173`. При работе через Vite/nginx proxy CORS не требуется. Настройки применяются после перезапуска backend.

## Локальная разработка

Нужны Python 3.12–3.14 и Node.js 22.12+; версии зависимостей зафиксированы в lock-файлах. Первый терминал, PowerShell, из корня репозитория:

```powershell
Copy-Item .env.example .env
cd backend
python -m venv .venv
.venv/Scripts/python.exe -m pip install -r requirements-dev.lock
.venv/Scripts/python.exe -m pip install --no-deps -e .
.venv/Scripts/python.exe -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
```

Если `.env` уже настроен, не копируйте файл повторно. Для Linux/macOS используйте `cp .env.example .env`, `python3 -m venv .venv` и `.venv/bin/python` вместо `.venv/Scripts/python.exe`. Для локального LLM без Docker замените `host.docker.internal` на `localhost` в `.env`.

Второй терминал, из корня:

```sh
cd frontend
npm ci
npm run dev
```

Интерфейс: [localhost:5173](http://localhost:5173). Vite перенаправляет `/api` на backend. Если PowerShell блокирует `npm.ps1`, используйте `npm.cmd`. Для просмотра production-сборки: `npm run build`, затем `npm run preview`; backend должен продолжать работать, адрес интерфейса — [localhost:4173](http://localhost:4173).

## Контракты и проверки

Источник внешнего API — `backend/app/schemas.py`, ответа модели — `backend/app/content_policy.py`. Готовые файлы для команды:

- [contracts/openapi.json](contracts/openapi.json) — HTTP API, включая ошибки.
- [contracts/api.ts](contracts/api.ts) — типы для frontend.
- [contracts/llm-output.schema.json](contracts/llm-output.schema.json) — JSON модели.
- [examples/api.http](examples/api.http) — готовые запросы для REST Client и чтения.

После изменения схем, из корня, с установленными backend-зависимостями:

```powershell
backend/.venv/Scripts/python.exe scripts/export_contracts.py
backend/.venv/Scripts/python.exe scripts/export_contracts.py --check
```

Экспорт также записывает точную копию `contracts/api.ts` в `frontend/src/generated/api.ts`, чтобы типы входили в контекст Docker-сборки frontend. `frontend/src/types.ts` реэкспортирует эти типы и отдельно определяет только локальное состояние `DraftState`. Генерируемые файлы вручную не редактируются; `--check` проверяет обе копии. Проверки backend из `backend`:

## Развёртывание и сеть

Модель может работать на другом компьютере, в том числе в другой сети: шаги для его
владельца — [docs/REMOTE_MODEL.md](docs/REMOTE_MODEL.md). Что настроить у себя и что
обязательно сделать перед тем, как открыть сайт для других людей, —
[docs/DEPLOY.md](docs/DEPLOY.md). Замена локальной модели на GigaChat разобрана в
[docs/GIGACHAT.md](docs/GIGACHAT.md).

## Отчёт о качестве

`scripts/quality_report.py` прогоняет фиксированный набор случаев из
[examples/quality_cases.yaml](examples/quality_cases.yaml) и показывает, что стало с каждым
черновиком. Отчёт проверок и сверка с эталоном кейса — `scripts/qa_report.py`, описание в
[docs/QA.md](docs/QA.md).


```powershell
.venv/Scripts/python.exe -m ruff check .
.venv/Scripts/python.exe -m pytest -q
.venv/Scripts/python.exe -m pip check
```

Проверка frontend из `frontend`: `npm run build`. На Linux/macOS замените путь к Python на `.venv/bin/python`. Автотесты LLM работают на подменённом HTTP-клиенте без ключа и сети. GitHub Actions выполняет backend-проверки, проверку контрактов, сборку frontend и сквозную проверку Compose.

Автотесты проверяют API, транспорт, ошибки и кэш, а также структуру DOCX: восемь сочетаний типа и шаблона, сохранение исходных фактов, метки пропусков, введённые реквизиты, состав обязательных реквизитов по эталону, расположение реквизитов и колонтитулы обоих шаблонов, шрифты и язык стилей, посимвольное сохранение текста, отличие оформления без изменения текста, некорректные запросы и отказ обработки. Внешний вид DOCX в Word/LibreOffice и смысловые гарантии политики обработки проверяются отдельно по [сценариям передачи](docs/HANDOFF.md#проверка-перед-передачей).
Отдельный набор проверяет режим `llm` на расписанных ответах модели: приоритет ответов пользователя, обнуление неподтверждённого реквизита, один повтор, явную ошибку после повтора, сетевой сбой и сведение дат и сумм к одному виду. Запущенная Ollama для тестов не нужна.

## Что уже есть вокруг backend

Подключены React/TypeScript/Vite и три этапа работы, четыре типа документа, два оформления «Классический корпоративный»/«Современный регламентный», формы реквизитов из YAML, localStorage и скачивание DOCX. Смена оформления использует готовый результат и не вызывает обработку повторно. Пропущенные обязательные реквизиты получают метки, необязательные пропускаются.

### Попробовать вручную

1. Нажмите «Вставить пример» или используйте вход из `examples/service_memo.json`. Это вымышленные демонстрационные данные.
2. Выберите тип и оформление, заполните реквизиты либо оставьте часть пустой.
3. Подготовьте и скачайте DOCX. Убедитесь, что метки пропусков видны, а текст редактируется.
4. Вернитесь к реквизитам, смените только оформление, скачайте второй файл: содержание совпадёт.
5. Установите `TEXT_PROCESSOR=unavailable`, перезапустите backend и попробуйте подготовить изменённый черновик. Должна появиться ошибка. Верните `stub`, перезапустите backend и нажмите «Повторить подготовку».
6. Для проверки ИИ включите `TEXT_PROCESSOR=llm` с запущенной Ollama и подготовьте `examples/typos.txt`: текст должен стать грамотным, а даты, суммы и имена — остаться прежними.

Конфигурации типов и оформлений стартовые: сверка с эталонами относится к роли 3. Соответствие конкретному ГОСТ или стандарту организации не заявляется. Краткая постановка сохранена в [CONTEXT.md](docs/CONTEXT.md).

Результаты обработки временно хранятся только в ограниченном кэше процесса; DOCX создаётся в памяти запроса. Авторизации, серверной истории и постоянного хранилища нет. Проект рассчитан на локальную командную разработку и демонстрацию; публичный многопользовательский запуск требует отдельной настройки доступа и ограничений.

Для первого прохода команде достаточно прочитать [архитектуру и API](docs/ARCHITECTURE.md), затем [распределение задач и сценарии проверки](docs/HANDOFF.md). Исходный краткий контекст сохранён в [docs/CONTEXT.md](docs/CONTEXT.md).

Добавление типа: создайте `backend/app/doc_types/*.yaml` с уникальным `id`, `name`, `description`, `title`, `fields` и `blocks`. Необязательный `summary: true` у реквизита разрешает обработчику сформулировать значение по смыслу черновика: так помечена только тема документа, остальные поля заполняются лишь по прямому упоминанию. Каждый реквизит, а также `title` и `body` должны присутствовать в `blocks` ровно один раз. `show_title: false` убирает вид документа со страницы. Перезапустите backend; форма и генератор подхватят новый тип из каталога. Содержательные правила заполнения нового типа реализуются отдельно в обработчике.

У реквизита можно задать `placement` — место в документе — и `prefix` — постоянный текст перед значением, например `"№ "`. Соседние в `blocks` поля с одинаковым `placement` объединяются в один блок:

| `placement` | Как печатается |
| --- | --- |
| `labeled` (по умолчанию) | «Подпись поля: значение» |
| `letterhead` | Организация: в верхнем колонтитуле (`header: organization`) или вверху страницы |
| `addressee` | «Кому» / «От кого»: блок справа или слева, либо таблица (`recipient_layout: table`) |
| `registration` | Одна строка: `11.09.2026 № 47-СЗ` |
| `headline` | Заголовок к тексту без подписи поля |
| `salutation` | Обращение перед текстом, выравнивание как у заголовка документа |
| `paragraph` | Отдельный абзац после текста, например «Приложение: …» |
| `signature` | Должность и расшифровка подписи по `signature_alignment`: слева — в одну строку с табуляцией, по центру или справа — двумя строками |
| `executor` | Исполнитель в конце, мелким шрифтом |

Поля `letterhead`, `addressee`, `registration` и `signature` должны стоять в `blocks` подряд, иначе backend не запустится.

Добавление оформления: скопируйте YAML из `backend/app/templates`, задайте уникальный `id` и измените оформление. Кроме шрифта, полей и интервалов доступны `signature_alignment`, `recipient_layout` (`block` или `table`), `header` (`none` или `organization`), `footer` (`none`, `page_number` или `title_and_date`), `header_footer_font_size`, `letterhead_alignment`, `headline_alignment`, `headline_bold`, `addressee_width_mm` (ширина блока адресата при `recipient_alignment: right`) и `small_font_size` (исполнитель). Параметры валидируются при запуске. Текст и набор реквизитов в шаблон не помещаются. Бланки в две колонки или другие варианты колонтитулов потребуют расширить контракт Template и генератор.

Обработка за `TextProcessor` реализована в двух режимах: `openai` — штатный транспорт с политикой сохранения исходника, `llm` — обработчик роли 2 со строгой JSON-схемой, независимым извлечением и сверкой фактов и не более одним повтором при неверном ответе. Осталось: проверка качества на длинных документах.

Ограничения: нет авторизации, серверной истории, кэша, переживающего перезапуск, редактирования готового результата в интерфейсе и защиты для публичного многопользовательского размещения.
